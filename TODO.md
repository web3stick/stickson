# TODO

## Completed

- [x] Replace all npm references with bun in docs/INSTALL.md
- [x] Replace all npm references with bun in docs/DEV_DEPENDENCY.md
- [x] Fix sleet-css example to use @sleet-css/sleet-css via jsdelivr CDN
- [x] Fix examples so they run without dependency installation issues
- [x] Test examples build cleanly

## Next Steps (suggested)

- Schema audit: check content-schema.json for missing HTML elements (table, video, audio, canvas, svg, etc.)
- Improve error messages in CLI when JSON validation fails
- Add more examples (e.g., blog, landing page)
- Test the `stickson create` command works end-to-end
- Add `bun run build:examples` script to root package.json

=====================
<br/>
copyright 2026 by web3stick.near
